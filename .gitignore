node_modules/
dist/
.DS_Store
*.local
.cache/

# Evaluation fixtures are derived from the sample videos, which we do not
# redistribute. Regenerate with ./scripts/make-fixtures.sh
test/fixtures/frames/

# Never commit real keys. .env.example documents the variable instead.
.env
.env.local
.env.example